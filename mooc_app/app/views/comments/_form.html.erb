<%= form_for([@lecture, @lecture.comments.build]) do |f| %>


  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
